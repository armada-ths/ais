from django.conf.urls import url
from locations import views

urlpatterns = [
    # url(r'^$', views.location_list, name='location_list'),
    # url(r'^new/$', views.location_create, name='location_new'),
    # url(r'^edit/(?P<pk>\d+)$', views.location_update, name='location_edit'),
    # url(r'^delete/(?P<pk>\d+)$', views.location_delete, name='location_delete'),
]
