name: Validate format

on: [pull_request]

jobs:
  validate_format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
        with:
          options: "--check"
          src: "."
